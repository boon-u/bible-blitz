[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Supabase publishable/anon keys are designed for the browser bundle.
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_PUBLISHABLE_KEY,VITE_SUPABASE_ANON_KEY"
